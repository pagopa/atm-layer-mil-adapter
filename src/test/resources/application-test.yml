camunda:
  bpm:
    client:
      base-url: http://localhost:8080
spring:
  datasource:
    url: jdbc:h2:mem:test\;INIT=CREATE SCHEMA IF NOT EXISTS TEST\;SET SCHEMA TEST;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driverClassName: org.h2.Driver
    cache-configuration:
      cache-name: token-cache
      max-entries: 100
id-pay:
  api-key: "testIdPayToken"